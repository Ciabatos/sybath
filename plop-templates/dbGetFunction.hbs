// GENERATED CODE - DO NOT EDIT MANUALLY - dbGetFunction.hbs

"use server"
import { query } from "@/db/postgresMainDatabase/postgresMainDatabase"

{{{tsReturnType}}}

export async function get{{methodPascalName}}({{tsArgsList}}) {
  try {
    const result = await query(
      `SELECT * FROM {{schema}}.{{method}}({{sqlParamsPlaceholders}});`,
      [{{argsArray}}]
    )

    return result.rows as T{{methodPascalName}}Result[]
  } catch (error) {
    console.error("Error executing get{{methodPascalName}}:", error)
    throw new Error("Failed to execute get{{methodPascalName}}")
  }
}