// GENERATED CODE - DO NOT EDIT MANUALLY - hookGetTableServer.hbs
"use server"

import type { {{methodTypeName}}, {{indexTypeName}} } from "@/db/postgresMainDatabase/schemas/{{schema}}/{{tableCamelName}}"
import { {{fetcherName}} } from "@/methods/services/{{schema}}/{{fetcherName}}"

type TResult = {
  raw: {{methodTypeName}}[]
  byKey: {{indexTypeName}}
  apiPath: string
  atomName: string
}

export async function {{methodName}}Server(): Promise<TResult> {
  const { record } = await {{fetcherName}}()

  return {
    raw: record!.raw,
    byKey: record!.byKey,
    apiPath: `{{apiPathParams}}`,
    atomName: `{{tableCamelName}}Atom`,
  }
}
