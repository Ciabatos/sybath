// GENERATED CODE - DO NOT EDIT MANUALLY - hookGetTable.hbs

"use client"
import { {{typeName}}RecordBy{{typeRecordName}} } from "@/db/postgresMainDatabase/schemas/{{schema}}/{{tablePascalName}}"
import { {{indexMethodName}} } from "@/methods/functions/converters"
import { {{table}}Atom } from "@/store/atoms"
import { useAtomValue, useSetAtom } from "jotai"
import { useEffect, useRef } from "react"
import useSWR from "swr"

export function useFetch{{tablePascalName}}() {
  const {{table}} = useAtomValue({{table}}Atom)
  const set{{tablePascalName}} = useSetAtom({{table}}Atom)
  const { data } = useSWR("/api/{{table}}", { refreshInterval: 3000 })

  const prevDataRef = useRef<unknown>(null)

  useEffect(() => {
    if (data === undefined) return
    if (JSON.stringify(prevDataRef.current) !== JSON.stringify(data)) {
      const index = data ? ({{{indexMethodName}}}({{{indexMethodArgs}}}, data) as {{typeName}}RecordBy{{typeRecordName}}) : {}
      set{{tablePascalName}}(index)
      prevDataRef.current = data
    }
  }, [data, set{{tablePascalName}}])

  return { {{table}} }
}
