/**
 * Generator jest w pełni automatyczny — brak pytań.
 * Plop wymaga jednak, żeby pole `prompts` istniało; zwracamy pusty obiekt.
 */
export async function getFormatPrettierPrompts() {
  return {}
}
