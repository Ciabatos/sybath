// GENERATED CODE - DO NOT EDIT MANUALLY - hookGetTableServer.hbs
"use server"

import type { {{methodTypeName}}, {{indexTypeName}} } from "@/db/postgresMainDatabase/schemas/{{schema}}/{{tableCamelName}}"
import { {{fetcherName}}Service } from "@/methods/services/{{schema}}/{{fetcherName}}Service"

type TResult = {
  raw: {{methodTypeName}}[]
  byKey: {{indexTypeName}}
  apiPath: string
  atomName: string
}

export async function {{methodName}}Server(options?: { forceFresh?: boolean }): Promise<TResult> {
  const { record } = await {{fetcherName}}Service({ forceFresh: options?.forceFresh })

  return {
    raw: record!.raw,
    byKey: record!.byKey,
    apiPath: `{{apiPathParams}}`,
    atomName: `{{tableCamelName}}Atom`,
  }
}
