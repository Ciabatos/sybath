{{{match}}}
  "Destructure use{{schemaTablePascalName}}ByKey": {
    "prefix": "hook_{{schema}}_core_use{{schemaTablePascalName}}ByKey",
    "body": [
      "useFetch{{schemaTablePascalName}}ByKey({{#if methodParamsColumns}}{ {{#each methodParamsColumns}}{{camelName}}{{#unless @last}}, {{/unless}}{{/each}} }{{/if}})",
      "const {{tableCamelName}} = use{{schemaTablePascalName}}()"
    ],
    "description": "Destructuring use{{schemaTablePascalName}}ByKey hook"
  },