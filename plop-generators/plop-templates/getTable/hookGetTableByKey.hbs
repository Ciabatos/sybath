// GENERATED CODE - DO NOT EDIT MANUALLY - hookGetTableByKey.hbs

"use client"
import { {{indexTypeName}}, {{methodTypeName}}{{#if methodParamsColumns}}, {{methodParamsTypeName}}{{/if}} } from "@/db/postgresMainDatabase/schemas/{{schema}}/{{tableCamelName}}"
import { {{indexMethodName}} } from "@/methods/functions/util/converters"
import { {{tableCamelName}}Atom } from "@/store/atoms"
import { useSetAtom } from "jotai"
import { useEffect } from "react"
import useSWR from "swr"

export function useFetch{{schemaTablePascalName}}ByKey({{#if methodParamsColumns}} params: {{methodParamsTypeName}} {{/if}}) {
  const set{{schemaTablePascalName}} = useSetAtom({{tableCamelName}}Atom)
  
  const { data } = useSWR<{{methodTypeName}}[]>(`{{apiPathParamsByKey}}`, { refreshInterval: 3000 })

  const {{tableCamelName}} = data
  ? ({{{indexMethodName}}}({{{indexMethodParams}}}, data) as {{indexTypeName}})
  : {}

  useEffect(() => {
    if ({{tableCamelName}}) {
      set{{schemaTablePascalName}}({{tableCamelName}})
    }
  }, [{{tableCamelName}}, set{{schemaTablePascalName}}])

  return { {{tableCamelName}} }
}
