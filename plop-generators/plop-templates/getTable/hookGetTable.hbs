// GENERATED CODE - DO NOT EDIT MANUALLY - hookGetTable.hbs

"use client"
import { {{indexTypeName}}, {{methodTypeName}} } from "@/db/postgresMainDatabase/schemas/{{schema}}/{{entityCamelName}}"
import { {{indexMethodName}} } from "@/methods/functions/util/converters"
import { {{entityCamelName}}Atom } from "@/store/atoms"
import { useAtomValue, useSetAtom } from "jotai"
import { useEffect } from "react"
import useSWR from "swr"

export function useFetch{{schemaEntityPascalName}}() {
  const set{{schemaEntityPascalName}} = useSetAtom({{entityCamelName}}Atom)
  
  const { data } = useSWR<{{methodTypeName}}[]>(`{{apiPathParams}}`, { refreshInterval: 3000 })

  useEffect(() => {
    if (data) {
      const {{entityCamelName}} = {{{indexMethodName}}}({{{indexMethodParams}}}, data) as {{indexTypeName}}
      set{{schemaEntityPascalName}}({{entityCamelName}})
    }
  }, [data, set{{schemaEntityPascalName}}])
}

export function use{{schemaEntityPascalName}}State() {
  return useAtomValue({{entityCamelName}}Atom)
}