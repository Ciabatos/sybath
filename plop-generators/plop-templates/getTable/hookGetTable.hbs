// GENERATED CODE - DO NOT EDIT MANUALLY - hookGetTable.hbs

"use client"
import { {{indexTypeName}}, {{methodTypeName}} } from "@/db/postgresMainDatabase/schemas/{{schema}}/{{tableCamelName}}"
import { {{indexMethodName}} } from "@/methods/functions/util/converters"
import { {{tableCamelName}}Atom } from "@/store/atoms"
import { useSetAtom } from "jotai"
import { useEffect } from "react"
import useSWR from "swr"

export function useFetch{{schemaTablePascalName}}() {
  const set{{schemaTablePascalName}} = useSetAtom({{tableCamelName}}Atom)
  
  const { data } = useSWR<{{methodTypeName}}[]>(`{{apiPathParams}}`, { refreshInterval: 3000 })

  useEffect(() => {
    if (data) {
      const {{tableCamelName}} = {{{indexMethodName}}}({{{indexMethodParams}}}, data) as {{indexTypeName}}
      set{{schemaTablePascalName}}({{tableCamelName}})
    }
  }, [data, set{{schemaTablePascalName}}])
}
