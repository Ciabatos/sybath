// GENERATED CODE - SHOULD BE EDITED MANUALLY TO END CONFIGURATION - actionGetMethodAction.hbs
"use server"

import { auth } from "@/lib/auth"
import { T{{entityPascalName}}ServiceParams, {{entityCamelName}}Service } from "@/methods/services/{{schema}}/{{entityCamelName}}Service"
import { headers } from "next/headers"

type T{{entityPascalName}}ActionParams = Omit<T{{entityPascalName}}ServiceParams, "sessionUserId">

export async function {{actionName}}(params: T{{entityPascalName}}ActionParams) {
  try {
    const session = await auth.api.getSession({ headers: await headers() })
    const sessionUserId = session?.user?.id

    if (!sessionUserId) {
      return {
        status: false,
        message: "Active player mismatch",
      }
    }

    //MANUAL CODE - START

    //MANUAL CODE - END

    const data: T{{entityPascalName}}ServiceParams = {
      sessionUserId: sessionUserId,
      ...params,
    }

    const result = await {{entityCamelName}}Service(data)
    return result
  } catch (error) {
    console.error("Error {{actionName}} :", {
      error,
      params,
      timestamp: new Date().toISOString(),
    })

    return {
      status: false,
      message: "Unexpected error occurred. Please refresh the page.",
    }
  }
}