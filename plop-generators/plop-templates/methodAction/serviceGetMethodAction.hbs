// GENERATED CODE - SHOULD BE EDITED MANUALLY TO END CONFIGURATION - serviceGetMethodAction.hbs
"use server"

import { {{methodParamsTypeName}}, {{entityCamelName}} } from "@/db/postgresMainDatabase/schemas/{{schema}}/{{entityCamelName}}"

//MANUAL CODE - START

export type T{{entityPascalName}}ServiceParams = {
userId: string
playerId: number
}

//MANUAL CODE - END

export async function {{entityCamelName}}Service(params: T{{entityPascalName}}ServiceParams) {
  try {
    const userId = params.userId
    const playerId = params.playerId

    //MANUAL CODE - START

    {{#each methodParamsColumns}}
    const {{camelName}} =
    {{/each}}

    //MANUAL CODE - END

    const data: {{methodParamsTypeName}} = {
      {{#each methodParamsColumns}}
      {{camelName}}: {{camelName}},
      {{/each}}
    }

    const result = await {{entityCamelName}}(data)
    return result
  } catch (error) {
    console.error("Error {{entityCamelName}}Service :", {
      error,
      params,
      timestamp: new Date().toISOString(),
    })

    return {
      status: false,
      message: "Unexpected error occurred. Please refresh the page.",
    }
  }
}